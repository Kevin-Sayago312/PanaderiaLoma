{% extends 'base_admin.html.twig' %}

{% block title %} {{parent()}} - Administrador {% endblock %}

{% block body %}
{{parent()}}
<!-- EMPIEZA SECCION DE APARTADOS -->
<div class="config-perfil mt-150 mb-5">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <div class="card shadow-lg">
                    <div class="card-header bg-primary text-white">
                        <h3 class="text-center mb-0">Nuestros apartados</h3>
                    </div>
                    <div class="card-body">
                        <ul class="list-group list-group-flush mb-5">
                            <li class="list-group-item d-flex justify-content-between align-items-center mt-5 ">
                                <h4 class="mb-5">Productos de la panadería</h4>
                                <a href="{{ path('app_add_categorias') }}" class="btn btn-outline-primary mb-5">
                                    Ver más <i class="fas fa-chevron-right ml-1"></i>
                                </a>
                            </li>
                            <li class="list-group-item d-flex justify-content-between align-items-center mt-5">
                                <h4>Categorías de la panadería</h4>
                                <a href="{{ path('app_add_productos') }}" class="btn btn-outline-primary">
                                    Ver más <i class="fas fa-chevron-right ml-1"></i>
                                </a>
                            </li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<!-- TERMINA SECCION DE APARTADOS -->

<!-- Incluir la CDN de Font Awesome para los íconos -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">


{% endblock %}
